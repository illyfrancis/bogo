describe("Given AccountCriteria model", function() {

	it("should be valid", function() {

/*		var paginatedAccounts = new app.collections.PaginatedAccounts();
		paginatedAccounts.add({name:"zero", number:900, selected:false});
		paginatedAccounts.add({name:"one", number:901, selected:false});
		paginatedAccounts.add({name:"two", number:902, selected:false});
		paginatedAccounts.add({name:"three", number:903, selected:false});
		paginatedAccounts.add({name:"four", number:904, selected:false});
		paginatedAccounts.add({name:"five", number:905, selected:false});
		paginatedAccounts.add({name:"six", number:906, selected:false});
		paginatedAccounts.add({name:"seven", number:907, selected:false});
		paginatedAccounts.add({name:"eight", number:908, selected:false});
		paginatedAccounts.add({name:"nine", number:909, selected:false});

		paginatedAccounts.fetch();
		paginatedAccounts.pager();


		var accountCriteria = new app.models.AccountCriteria({filters: paginatedAccounts});

		expect(accountCriteria.isValid()).toBeTruthy();

		console.log("> before : " + accountCriteria.get("isApplied"));

		accountCriteria.set("isApplied", true);

		console.log("> after : " + accountCriteria.get("isApplied"));

		expect(accountCriteria.get("isApplied")).toBe(false);

		// new test
		paginatedAccounts.at(0).set("selected", true);

		accountCriteria.set("isApplied", true);

		console.log("> after : " + accountCriteria.get("isApplied"));

		expect(accountCriteria.get("isApplied")).toBe(true);

		// new test
		var cr = accountCriteria.criteria();

		console.log(">>> criteria is " + JSON.stringify(cr));
*/
	});

});